pluginManagement {
    repositories {
        maven { url = uri("${settingsDir}/build-libs/m2") }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven { url = uri("${settingsDir}/build-libs/m2") }
        google()
        mavenCentral()
    }
}
rootProject.name = "Dulo TV"
include ':app'
